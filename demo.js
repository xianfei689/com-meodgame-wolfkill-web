/**
 * Particleground demo
 * @author Jonathan Nicol - @mrjnicol
 */

$(document).ready(function() {
  $('#splash-container').particleground({
    dotColor: '#5cbdaa',
    lineColor: '#5cbdaa'
  });
  // $('.intro').css({
  //   'margin-top': -($('.intro').height() / 2)
  // });
});